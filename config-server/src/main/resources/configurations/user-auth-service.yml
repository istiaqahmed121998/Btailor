spring:
  application:
    name: auth-user-service
  datasource:
    url: jdbc:postgresql://ep-dry-frost-a5mysqyy-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_egt3Q8bFWNcZ
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    url: jdbc:postgresql://ep-dry-frost-a5mysqyy-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
    user: neondb_owner
    password: npg_egt3Q8bFWNcZ
    locations: classpath:db/migration
    baseline-on-migrate: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - profile
              - email

server:
  port: 8082
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: always
    whitelabel:
      enabled: false
