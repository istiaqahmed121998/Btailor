spring.application.name=bTailor

spring.datasource.url=jdbc:postgresql://localhost:5432/btailor
spring.datasource.username=zenithjhony
spring.datasource.password=123456
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Database Migrations
spring.flyway.url=jdbc:postgresql://localhost:5432/btailor
spring.flyway.user=zenithjhony
spring.flyway.password=123456
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

  # Ensures Flyway can run if the database is empty
